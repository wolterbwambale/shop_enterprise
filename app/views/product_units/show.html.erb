<div class="container">
<h2>unit</h2>
<p><%=@product_unit.code%></p>
<p><%=@product_unit.title%></p>
<p><%=@product_unit.description%></p>

<%= link_to "Edit", edit_product_unit_path%>


<%= link_to "Delete", product_unit_path(@product_unit), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure, you want to delete #{@product_unit.title}?"
                  } %>
<%= link_to 'back',{ action: :index }%>
</div>